# Blind agent + PPO on easy with 2x128 net (default ent_coef=0.01).
# Direct comparison to labeled-ppo-easy-128 (76%).
# Tests whether blind > labeled holds at intermediate capacity.
variant: blind
algo: ppo
profile: easy
total_steps: 4_000_000
video_freq: 250_000
run_dir: /media/hdd1/physics-priors-latent-space/lunar-lander-networks/rl_agents/blind-ppo-easy-128-Feb122026
n_envs: 8

# Intermediate: 2 hidden layers of 128 units (shared actor/critic, ~32K params).
net_arch: [128, 128]
