# Blind agent + PPO on fixed Gymnasium default physics (no randomization).
# Control condition for blind variant — single physics config.
variant: blind
algo: ppo
profile: gym-default
total_steps: 3_000_000
video_freq: 250_000
early_stop_landed_pct: 95.0
early_stop_patience: 4
run_dir: /media/hdd1/physics-priors-latent-space/lunar-lander-networks/rl_agents/blind-ppo-gym-default-256-Feb122026
n_envs: 8

# Research network: separate 3x256 actor and critic (~200K params).
net_arch:
  pi: [256, 256, 256]
  vf: [256, 256, 256]
