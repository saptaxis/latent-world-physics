# History agent + PPO: stacked frames (120D obs = 8 * kinematic + rays).
# Infers physics from trajectory context, no explicit physics params.
variant: history
algo: ppo
profile: easy
total_steps: 3_000_000
video_freq: 500_000
run_dir: runs/lunar_lander/history-ppo
